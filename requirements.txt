# Core: Temporal Python SDK (workflows, activities, worker, in-process test server)
temporalio>=1.30,<2

# Dev / test
pytest>=9.1.1
pytest-asyncio>=0.24

# Optional: real-LLM path (set AGENT_PROVIDER=anthropic and ANTHROPIC_API_KEY).
# The demo runs fully offline with the built-in MockProvider without this.
# anthropic>=0.40
